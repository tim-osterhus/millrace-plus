# Millrace Plus Scaffold Smoke Prompt

This is scaffold smoke content for PLUS-0001. It is a data-only fixture used to verify workflow package bytes can be read, imported, listed, and inspected through public WPKG APIs.

It does not claim runtime, package, import, routing, retry, capability, runner, provider, plugin, or registry authority. It is not an official workflow prompt.
